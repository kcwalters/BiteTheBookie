@using BiteTheBookie.Helpers
@model IEnumerable<BiteTheBookie.ViewModels.MLBOddsViewModel>

@{
    ViewData["Title"] = "MLB Odds";
}

<style>
    .odds-table {
        table-layout: fixed;
        width: 100%;
    }

    .odds-table th,
    .odds-table td {
        text-align: center;
        vertical-align: middle;
        overflow: hidden;
    }

    .col-game      { width: 16%; }
    .col-time      { width: 14%; }
    .col-odds      { width: 23%; }
    .col-source    { width: 8%;  }

    .odds-column {
        padding-left: 20px;
        padding-right: 20px;
    }

    .odds-row {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 6px;
        width: 100%;
    }

    .odds-item {
        display: flex;
        justify-content: space-between;
        align-items: center;
        width: 100%;
    }

    .odds-item .team-label {
        margin-right: 10px;
        white-space: nowrap;
        text-align: left;
        flex: 1 1 auto;
        overflow: hidden;
        text-overflow: ellipsis;
    }

    .odds-item .badge {
        flex: 0 0 auto;
        min-width: 60px;
        text-align: center;
    }

    .odds-table tbody tr {
        cursor: pointer;
        transition: background-color 0.2s;
    }

    .odds-table tbody tr:hover {
        background-color: rgba(13, 110, 253, 0.1) !important;
    }
</style>

<div class="container py-4">
    <h2 class="mb-4">MLB Odds</h2>

    @if (!Model.Any())
    {
        <div class="alert alert-info">
            <p class="mb-0">No MLB odds available at this time. Please check back later or ensure your Odds API key is configured.</p>
        </div>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-striped table-hover odds-table">
                <thead class="table-dark">
                    <tr>
                        <th class="col-game">Game</th>
                        <th class="col-time">Time</th>
                        <th class="col-odds odds-column">Moneyline</th>
                        <th class="col-odds odds-column">Spread</th>
                        <th class="col-odds odds-column">Total</th>
                        <th class="col-source">Source</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var game in Model)
                    {
                        <tr data-href="https://sportsbook.draftkings.com/leagues/baseball/mlb" onclick="window.open(this.dataset.href, '_blank')">
                            <td class="col-game">
                                <div class="fw-bold">@game.AwayTeam</div>
                                <div class="text-muted">&#64; @game.HomeTeam</div>
                            </td>
                            <td class="col-time">
                                @VenueTimeZoneHelper.FormatVenueTime(game.CommenceTime, game.VenueTimeZoneId)
                            </td>
                            <td class="col-odds odds-column">
                                <div class="odds-row">
                                    <div class="odds-item">
                                        <span class="team-label">@game.AwayTeam:</span>
                                        <span class="badge bg-primary">@game.AwayMoneyline</span>
                                    </div>
                                    <div class="odds-item">
                                        <span class="team-label">@game.HomeTeam:</span>
                                        <span class="badge bg-primary">@game.HomeMoneyline</span>
                                    </div>
                                </div>
                            </td>
                            <td class="col-odds odds-column">
                                <div class="odds-row">
                                    @if (!string.IsNullOrEmpty(game.AwaySpread))
                                    {
                                        <div class="odds-item">
                                            <span class="team-label">@game.AwayTeam:</span>
                                            <span class="badge bg-success">@game.AwaySpread (@game.AwaySpreadPrice)</span>
                                        </div>
                                    }
                                    @if (!string.IsNullOrEmpty(game.HomeSpread))
                                    {
                                        <div class="odds-item">
                                            <span class="team-label">@game.HomeTeam:</span>
                                            <span class="badge bg-success">@game.HomeSpread (@game.HomeSpreadPrice)</span>
                                        </div>
                                    }
                                </div>
                            </td>
                            <td class="col-odds odds-column">
                                <div class="odds-row">
                                    @if (!string.IsNullOrEmpty(game.OverPoint))
                                    {
                                        <div class="odds-item">
                                            <span class="team-label">O @game.OverPoint:</span>
                                            <span class="badge bg-warning text-dark">@game.OverPrice</span>
                                        </div>
                                    }
                                    @if (!string.IsNullOrEmpty(game.UnderPoint))
                                    {
                                        <div class="odds-item">
                                            <span class="team-label">U @game.UnderPoint:</span>
                                            <span class="badge bg-warning text-dark">@game.UnderPrice</span>
                                        </div>
                                    }
                                </div>
                            </td>
                            <td class="col-source">
                                <small class="text-muted">@game.Bookmaker</small>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <div class="mt-3">
            <p class="text-muted small">
                <strong>Note:</strong> Odds are provided by @Model.FirstOrDefault()?.Bookmaker and are updated periodically.
                Please verify odds with your sportsbook before placing bets.
            </p>
        </div>
    }

    <div class="mt-4">
        <a asp-controller="Odds" asp-action="Index" class="btn btn-secondary">Back to Odds</a>
    </div>
</div>

@section Scripts {
    <script src="~/js/home-page.js" asp-append-version="true"></script>
}
