name: Trigger auto deployment for bitethebookiecontainer

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/bitethebookiecontainer-AutoDeployTrigger-dce7d7ed-5030-4cc0-a5ca-f934a772c0cb.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.BITETHEBOOKIECONTAINER_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.BITETHEBOOKIECONTAINER_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.BITETHEBOOKIECONTAINER_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: bitethebookie.azurecr.io
          registryUsername: ${{ secrets.BITETHEBOOKIECONTAINER_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.BITETHEBOOKIECONTAINER_REGISTRY_PASSWORD }}
          containerAppName: bitethebookiecontainer
          resourceGroup: BiteTheBookie
          imageToBuild: bitethebookie.azurecr.io/bitethebookiecontainer:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


