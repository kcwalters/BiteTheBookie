on:
  push:
    branches:
    - master
permissions:
  id-token: write
  contents: read
env:
  DOTNET_CORE_VERSION: 7.0.x
  CONTAINER_APP_NAME: bitethebookie-container-app
  CONTAINER_APP_ENVIRONMENT_NAME: managedEnv20260212174040
  RESOURCE_GROUP: bitethebookiebettingodds
  CONTAINER_REGISTRY_NAME: registry20260212175501
  CONTAINER_REGISTRY_LOGIN_SERVER: registry20260212175501.azurecr.io
jobs:
  BuildAndDeployContainerApp:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout source code
      uses: actions/checkout@v4
    - name: Setup .NET SDK
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_CORE_VERSION }}
    - name: Azure login
      uses: azure/login@v2
      with:
        client-id: ${{ vars.managedEnv20260212174040_CLIENT_ID_13C6 }}
        tenant-id: ${{ vars.managedEnv20260212174040_TENANT_ID_13C6 }}
        subscription-id: ${{ vars.managedEnv20260212174040_SUBSCRIPTION_ID_13C6 }}
    - name: Container App deploy
      uses: azure/container-apps-deploy-action@v1
      with:
        appSourcePath: ${{ github.workspace }}
        acrName: ${{ env.CONTAINER_REGISTRY_NAME }}
        acrUsername: ${{ secrets.registry20260212175501_USERNAME_13C6 }}
        acrPassword: ${{ secrets.registry20260212175501_PASSWORD_13C6 }}
        imageToBuild: ${{ env.CONTAINER_REGISTRY_LOGIN_SERVER }}/${{ env.CONTAINER_APP_NAME }}:${{ github.sha }}
        containerAppName: ${{ env.CONTAINER_APP_NAME }}
        containerAppEnvironment: ${{ env.CONTAINER_APP_ENVIRONMENT_NAME }}
        resourceGroup: ${{ env.RESOURCE_GROUP }}
