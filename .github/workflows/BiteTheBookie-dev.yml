on:
  push:
    branches:
    - master
permissions:
  id-token: write
  contents: read
env:
  DOTNET_CORE_VERSION: 7.0.x
  CONTAINER_APP_NAME: bitethebookie-container-app
  CONTAINER_APP_ENVIRONMENT_NAME: BiteTheBookie-dev
  RESOURCE_GROUP: BiteTheBookie
  CONTAINER_REGISTRY_NAME: bitethebookie
  CONTAINER_REGISTRY_LOGIN_SERVER: bitethebookie.azurecr.io
jobs:
  BuildAndDeployContainerApp:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout source code
      uses: actions/checkout@v4
    - name: Setup .NET SDK
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_CORE_VERSION }}
    - name: Azure login
      uses: azure/login@v2
      with:
        client-id: ${{ vars.BiteTheBookie_dev_CLIENT_ID_85DA }}
        tenant-id: ${{ vars.BiteTheBookie_dev_TENANT_ID_85DA }}
        subscription-id: ${{ vars.BiteTheBookie_dev_SUBSCRIPTION_ID_85DA }}
    - name: Container App deploy
      uses: azure/container-apps-deploy-action@v1
      with:
        appSourcePath: ${{ github.workspace }}
        acrName: ${{ env.CONTAINER_REGISTRY_NAME }}
        acrUsername: ${{ secrets.BiteTheBookie_USERNAME_85DA }}
        acrPassword: ${{ secrets.BiteTheBookie_PASSWORD_85DA }}
        imageToBuild: ${{ env.CONTAINER_REGISTRY_LOGIN_SERVER }}/${{ env.CONTAINER_APP_NAME }}:${{ github.sha }}
        containerAppName: ${{ env.CONTAINER_APP_NAME }}
        containerAppEnvironment: ${{ env.CONTAINER_APP_ENVIRONMENT_NAME }}
        resourceGroup: ${{ env.RESOURCE_GROUP }}
