on:
  push:
    branches:
    - master
permissions:
  id-token: write
  contents: read
env:
  DOTNET_CORE_VERSION: 7.0.x
  CONTAINER_APP_NAME: bitethebookie-container-app
  CONTAINER_APP_ENVIRONMENT_NAME: bitethebookieEnvDev
  RESOURCE_GROUP: BiteTheBookie
  CONTAINER_REGISTRY_NAME: bitethebookiecontanerregistrydev
  CONTAINER_REGISTRY_LOGIN_SERVER: bitethebookiecontanerregistrydev.azurecr.io
jobs:
  BuildAndDeployContainerApp:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout source code
      uses: actions/checkout@v4
    - name: Setup .NET SDK
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_CORE_VERSION }}
    - name: Azure login
      uses: azure/login@v2
      with:
        client-id: ${{ vars.bitethebookieEnvDev_CLIENT_ID_859D }}
        tenant-id: ${{ vars.bitethebookieEnvDev_TENANT_ID_859D }}
        subscription-id: ${{ vars.bitethebookieEnvDev_SUBSCRIPTION_ID_859D }}
    - name: Container App deploy
      uses: azure/container-apps-deploy-action@v1
      with:
        appSourcePath: ${{ github.workspace }}
        acrName: ${{ env.CONTAINER_REGISTRY_NAME }}
        acrUsername: ${{ secrets.bitethebookiecontanerregistrydev_USERNAME_859D }}
        acrPassword: ${{ secrets.bitethebookiecontanerregistrydev_PASSWORD_859D }}
        imageToBuild: ${{ env.CONTAINER_REGISTRY_LOGIN_SERVER }}/${{ env.CONTAINER_APP_NAME }}:${{ github.sha }}
        containerAppName: ${{ env.CONTAINER_APP_NAME }}
        containerAppEnvironment: ${{ env.CONTAINER_APP_ENVIRONMENT_NAME }}
        resourceGroup: ${{ env.RESOURCE_GROUP }}
